# 工作区整理与目标约束实施计划

目标：让任意新会话迅速知道唯一研究目的、当前差距、行动理由和下一条合法任务；保留原始实验可追溯性。

架构：根目录放入口；源码、实验、测试、模型、数据、证据分类存放。PLAN.md 是总计划，project/plan.json 是机器任务状态，STATUS.md 是当前快照。历史计划和失败证据保持原位、原文；已撤回结论不再占据当前入口。

- [x] 独立核对1h/2h原始步记录、心跳、checkpoint与lab日志；先出更正证据，不执行新训练。
- [x] 用 tools/reorganize_workspace.py 生成逐文件旧路径→新路径及SHA256；保存原源码/纲领/工作树差异；同盘移动并逐项验证哈希，不删除原始证据。
- [x] 用 run.py/project_paths.py 统一入口及旧文件名查找；迁移后仅修导入/项目根与日志路径，模型方程、残差和训练规则不变。旧运行禁止依靠路径兼容绕过源码/协议身份。
- [x] 重写AGENTS/README/CLAUDE/STATUS，增加PLAN和文件索引。任务要有目标、理由、预期、判据、证据及失败后去向。
- [x] 增加轻量harness登记、检查、状态与待办查询；验证前置关系和证据路径，不冒充科学认证。
- [x] 核验全部移动文件字节、核心数值路径、关键合同测试、导入及harness负测试；全部出数包lab_log。
- [x] 给出下一轮大步长、事件驱动执行指令；整理验收不代表论文验收，不启动研究训练。

交付：evidence/workspace_reorganization_20260914/REPORT.md；出数与验证见lab_log #264–#275。历史固定输出主入口封存，未承诺所有旧脚本可按旧命令重跑。

用户已经授权完成核查、目录整理和总计划；本次不另设例行审批。路径变更全部留痕，未覆盖旧FAIL。
