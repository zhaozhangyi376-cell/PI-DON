# SR-READBACK 精度一致性复核

SR-LOCAL初次输出保留在local_review。读回脚本曾直接把checkpoint中float64参考传给测量，而原_step_summary先转DUT float32；近零分量的nMAE分母对这个差异敏感。

独立新任务SR-READBACK：0参数更新、0closure，按原测量路径将参考转为DUT dtype，再读相同五臂64/128最多8个已存在快照；摘要重算保持同一输入。输出local_review_v2，禁止覆盖初次报告。原参考数组和权重不写回。

逐个保存差值和非有限项。允许零参考nMAE为None，其他非有限保留无效标志；绝对指标差≤1e-8且步号一致才认读回一致。任何不一致只作诊断，不改原FAIL。初次结果标为参考精度不匹配的诊断，不能作为接口损坏结论。

读取成本和摘要0更新；全部出数经harness/lab_log。两次本地诊断不消耗服务器15次临时更新预算。首批服务器协议与原始科学门槛均不修改。
