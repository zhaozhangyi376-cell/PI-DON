# 固定权重旋度算子与PEC审计协议

## 共同约束

- 输出目录固定为 `evidence/briefing_20260917/operator_audit` 和 `evidence/briefing_20260917/pec_audit`。
- 两项均为诊断，参数更新数必须为0；不得覆盖旧权重、旧SR-COMPARE或128步证据。
- 解析旋度和精确Yee只作参考，不计为DCO成绩。
- nMAE、相对L2、论文式Eq.(5) MRE分栏保存；近零/严格零参考保留绝对误差和计数。
- 新数值运行必须在行动登记后由 `lab_log.py run` 包裹。

## BRIEF-OP

问题：已有旧DCO、S1R与同结构随机权重，在同一输入上的固定权重旋度预测分别如何；预训练平面波到腔体首个非零输入之间是否存在明显分布/接口失配？

固定对象：

- `assets/models/dco_lr1e3_300.pt`；
- 可解析到的S1R `best.pt`，若本地只有服务器摘要而无权重则记INCOMPLETE，不用其他权重顶替；
- 与目标模型相同结构、固定种子2026091704的随机权重，仅作零训练基准；
- 一个固定的论文Fig.5参数解析案例；
- 一个固定新种子解析平面波案例；
- 已保存128轨迹的首个非零E拟合输入与Yee目标，若原始张量缺失则明确记INCOMPLETE，不重新训练重建。

成功交付：每个实际可用模型的参数哈希运行前后相同、`parameter_updates=0`、输入/真值/预测/误差图和指标JSON齐全。诊断PASS不等于论文精度PASS。

失败后：缺权重或缺输入保存为INCOMPLETE；仍完成可用对象和PEC独立任务，不补训、不替换证据。

## BRIEF-PEC

问题：当前PEC投影清零哪些Yee自由度，当前curl-E插入清零哪些面；论文所述“更新预测后计算loss”与当前计算图在哪些位置可确认或不能确认？

固定范围：CPU、小网格、生产 `Solver`/`PECCavity`、无优化器step。梯度示意只用独立小张量比较 `loss(P y_hat,y)` 与 `loss(y_hat,y)`，不宣称还原作者代码。

成功交付：六面切向E掩码、curl-E边界支持、梯度支持差异、源码定位和图均可由测试复算；生产源文件哈希前后相同。

失败后：保留失败测试和原始数组，不修改生产边界；转汇报中的“尚待作者实现确认”。
