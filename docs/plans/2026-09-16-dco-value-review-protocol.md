# DCO value retrospective (zero updates)

Task: SR-DCO-REVIEW. User requested pausing training after random128.
Gap: establish what the current pretrained/random comparison can support,
and distinguish operator accuracy, initialization benefit and solver utility.

Inputs: returned random128 ZIP, existing clean128 import, original DCO asset,
paper text, saved production sources and server ledgers. No parameter updates,
no resumed solver and no new trajectory. CPU checkpoint reads, array metric
remeasurements and plots only. Maximum 30 minutes for this audit invocation.
Output: evidence/server_resource_v1/dco_value_review_20260916/ (new only).

Recompute row costs and registered component/Q gates; verify returned output
hashes, action/lab identities, checkpoint cursor and saved fields at 32/64/128.
Compare frozen configs and production source hashes. Record limitations rather
than filling missing evidence. The record audit can PASS with scientific FAIL.
Scientific thresholds remain those of the original run. No 1024/8192 unlock.

Hypotheses are retrospective, not new confirmatory tests: preprocessing and
target mismatch, input distribution shift, optimization interference and
pretraining benefit limited to other tasks. Specify discriminating future
experiments in the report but do not launch or schedule them in this review.
