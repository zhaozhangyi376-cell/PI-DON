# PLAN — PI-DON 唯一总计划

更新：2026-09-17。随机128后的旧配方长程训练继续暂停；并行启动固定权重算子/PEC审计、详细汇报和独立论文显式配置第一阶段参考。机器状态见 [project/plan.json](project/plan.json)，当前证据见 [STATUS](STATUS.md)。此前快照保存在 `evidence/server_resource_v1/dco_value_review_20260916_r2/`，原实验协议保持冻结。

## 目标和现状

以可追溯证据复现“学旋度 → 每时间步适配 → 可靠电磁场”，分别判断论文指标、预训练成本和冻结复用收益。

| 层次 | 已有结果 | 当前结论 | 下一步要回答的问题 |
|---|---|---|---|
| 第一阶段 DCO | 旧模型有较好的旧口径测试；服务器S1R已完成1000轮、25000更新，开发宏nMAE 2.1864%，32³盲测4.1503%；独立`_01`论文显式参考线已完成25000更新并完成失败诊断 | S1R原始服务器证据审计PASS但科学FAIL；固定权重审计中旧lr1e3 Fig.5宏nMAE 0.312%，固定随机网络21.332%，说明旧DCO确实学到非平凡旋度映射；`PAPER01-S1`工程PASS但科学FAIL，宏nMAE=9.2334%、relL2 p90=57.9028%；诊断显示curl-z主导、近奇异角组最差、末段收敛很慢；数据合同审计确认`k dot E0`横向性自洽，但Eq.(4)带来Ez放大（p90=3.09，max=7.89）且近奇异角组误差最高 | 不把`PAPER01-S1`用于第二阶段；若继续第一阶段，应登记小型角度/幅值/归一化消融，而不是原样重训 |
| 第二阶段逐步适配 | 预训练和随机初始化均完成128残差接受；Q分别3.0811%/3.1335%；两组Ex/Ey分量门均失败 | 两组32/64/128快照读回与记录一致；同在69步首次分量门失败 | 暂停新训练，先回顾算子迁移和实现差异；不启动1024/8192 |
| 预训练收益 | 当前128配对：预训练261209 Adam、11074秒；随机159240 Adam、6837秒；固定算子对照0更新 | 当前配方不支持预训练成本收益，但不等于DCO没学到旋度；旧DCO在首个腔体E输入上global relL2 7.12，暴露训练分布到在线分布的迁移失配 | `PAPER01-S1`未过第一阶段门且诊断指向z分量/近奇异角/幅值问题；先修第一阶段定义再谈收益 |
| 长程/复用 | 合格128轨迹缺失 | 1024/8192/U均NOT_RUN | 新128全场和分量门均通过后才启动 |

S1R是第一阶段DCO训练。旧局部成绩不被新盲测失败抹除，但不同数据/指标不能拼成同一成绩。旧S1中断仍为INCOMPLETE，不能改判为S1R结果。

## 汇报前优先级

用户明后天汇报，当前方向收敛为“独立验证DCO算子，然后对齐原文明示实现”，不追加长时域。具体对照、历史理由复核、PEC计算顺序和四个有界工作包见[汇报前方案](docs/reports/2026-09-16-fidelity-boundary-and-briefing-plan.md)。固定权重算子审计、PEC计算图审计和逐步汇报素材均已完成；用户已要求PPT终稿暂停，现有r3/r4只作为素材底稿。独立第一阶段参考线的R1合同已纠正坐标单位分类和Eq.(4)幅值构造，并完成服务器25000更新回传审计、本地失败分解和数据/公式合同审计：工程完整，科学门失败；后继若继续应从角度拒绝阈值、Ez幅值构造和归一化口径做小消融，不是直接重训同配方。用户暂停旧配方长时域仍有效。

2026-09-17用户批准的四项中，`BRIEF-OP-R1`、`BRIEF-PEC`和`BRIEF-DECK`已完成，均未修改旧模型；旧`PAPER01-PREFLIGHT`保留，修订`PAPER01-PREFLIGHT-R1`已通过，`PAPER01-S1`已回传审计。它只从随机权重开始，原文明示项与未公开假设分栏登记；不是旧S1/S1R续跑，也不解锁1024/8192。

## 资源分工

| 资源 | 安排 | 约束 |
|---|---|---|
| GV100 32GB | 模型重测、训练和在线适配；GPU任务串行 | 首批FP32；比较microbatch4/8/16、等效batch32；以实测吞吐选配置 |
| 100核CPU | 数据生成、压缩、独立审计；小任务默认4线程，后续至多8进程且每进程1线程 | 防线程争抢；单时间轨迹不能拆成100段并行拼接 |
| 1TB内存 | 缓存训练/开发数组、只读参考，减少反复解压 | 本批每进程内存目标上限32GiB；不为占满内存扩大样本 |
| 百TB磁盘 | 原始数据、失败现场、不可变权重、源码/配置哈希 | 首批新增20GiB，服务器至少预留50GiB；旧证据永不清理 |
| 本地 | 源码检查、JSONL审计、单元测试和汇报 | 不覆盖服务器账本；主机名+lab_run_id+action_id联合标识运行 |

## 新队列

执行细则：[服务器首批协议](docs/plans/2026-09-15-server-resource-protocol.md)。新输出 `evidence/server_resource_v1/`。独立任务不因另一项科学失败取消。

| ID | 内容与预算 | 后续 | 当前安排 |
|---|---|---|---|
| SR-LOCAL | 用户S1R摘要、旧M2 steps/summary/64与128权重；0更新 | 误差分解、成本一致性、测量限制；不认证服务器未回传文件 | 本地PASS，初次读回由SR-READBACK补充 |
| SR-READBACK | 对齐原测量float32参考后读回8个64/128快照；0更新 | 初次参考精度不匹配现场保留；复核指标全部一致 | 本地PASS，见local_review_v2 |
| SR-COMPARE | S1R完整性检查；三模型×四网格×16同题重测；0更新 | S1R审计完整，旧lr1e3优于S1R；诊断集已暴露 | PASS |
| SR-PERF | 临时随机模型4/8/16微批，每配置2预热+3计时更新，共15 Adam、0closure | microbatch=8最快，4/8/16首步等价 | PASS |
| SR-DESIGN | 首批证据、论文/实现、标签、坐标和全时长场验收复核 | 冻结一个在线容差干预TOL1E5-P；暂不重复S1R | PASS |
| SR-S1 | 诊断支持单一干预后的独立训练 | 旧lr1e3已是当前更强候选；复训旧口径可作后续第一阶段交付 | BLOCKED；本批不消耗 |
| SR-SHORT | TOL1E5-P：旧lr1e3初始化，单步残差门1e-5，先64步，64场门过才同预算到128 | 有效重试R2第一个E_pending用满3000 Adam，残差比1.635e-5，0完整接受步；旧G128失败不改判 | FAIL |
| SR-FAIL-AUDIT | 0更新只读审计：比较TOL1E5-P首步失败与旧M2残差128/场失败 | 首个E目标身份匹配；A-P 1e-4在1220 Adam通过，TOL1E5-P 1e-5在3000 Adam失败；Q在57–58步越线，nMAE在66步越线 | PASS |
| SR-E1-BUDGET | 固定首个E目标，tol=1e-5，单次9000 Adam预算诊断 | 同一首个E目标在3769 Adam达到1e-5；诊断PASS，但不进入第二步，不解锁长程 | PASS |
| SR-MICRO4 | 旧lr1e3初始化，tol=1e-5，每半步9000 Adam，总72000 Adam，只跑4个完整步 | 4个完整严格步和第4步微型场门通过，累计10558 Adam；只授权另行登记8到16步 | PASS |
| SR-MICRO16 | 同配置延长到16个完整严格步，仍不进入64 | 16个完整严格步和第16步微型场门通过，累计13791 Adam；只授权另行登记64步验证 | PASS |
| SR-64 | 同配置延长到64个完整严格步，64步执行全场门 | 35步通过；第36步E半步9000 Adam后残差3.4467e-5，未过1e-5；未到旧57–58步场门瓶颈 | FAIL |
| SR-128 | 同配置延长到128个完整严格步，64和128均执行全场门 | 仅在SR-64回传PASS_64后运行；通过后也只授权另行登记1024，不直接启动8192 | BLOCKED |
| SR-64-LOWLR-CLEAN | 从0开始低学习率64步严格验证，64步执行全场门 | 完成64步，累计178204 Adam；64步Q=2.934%，六分量门通过 | PASS |
| SR-128-LOWLR-CLEAN | 从0开始低学习率128步严格验证，64和128均执行全场门 | 完成128步，累计261209 Adam；64步场门通过，128步Q=3.081%但Ex/Ey nMAE=1.5486%/1.6308%，第69步首次分量门失败 | 交付PASS / 科学FAIL |
| SR-128-PAPER-TOL-DIAG | 低学习率但半步残差门改为论文式1e-4；诊断速度与场误差 | 到64步停止；累计66173 Adam；64步Q=9.041%，field gate FAIL；不能作为长程门 | FAIL |
| SR-64-LOWLR-RANDOM | 随机初始化、低学习率、tol=1e-5，同clean64规则跑64步 | 完成64步，累计84881 Adam；64步Q=2.932%，六分量门通过；成本低于预训练clean64 | PASS |
| SR-128-LOWLR-RANDOM | 随机初始化、同clean128配置，独立从0跑128步 | 完成128步，累计159240 Adam；Q=3.1335%；Ex/Ey nMAE=1.7801%/1.7705%；第69步首次分量门失败 | 交付PASS / 科学FAIL |
| SR-DCO-REVIEW | 零更新回顾：随机回传哈希、两组快照、论文与源码 | 22项随机输出哈希和两组32/64/128快照一致；核心源码相同；冻结配置仅init不同；确认P使用旧300轮模型而非S1R | 审计PASS；暂停新训练 |
| BRIEF-OP / R1 | 固定旧lr1e3与固定随机网络，零更新同题算子测试 | 原行动因零参考分量聚合INCOMPLETE；R1只修报告空值并完成。Fig.5宏nMAE 0.312% vs 21.332%；首个腔体E上旧DCO global relL2 7.12 | R1诊断PASS；支持“学到旋度但在线分布迁移差” |
| BRIEF-PEC | 生产PEC掩膜、预测插入和玩具梯度计算图审计；0更新 | 三个E分量切向边界置零正确；硬投影后边界梯度为0，与论文所述loss路径不等价 | 诊断PASS；作者掩膜/张量对齐仍未知 |
| BRIEF-DECK | 汇总电磁理论、网络张量、每步适配和全部证据 | 44页可编辑PPT、逐页讲稿、Q&A及缩略总览；渲染44/44、无溢出 | PASS；当前汇报主文件为r3 |
| PAPER01-PREFLIGHT / R1 | 独立`_01`参考线CUDA预检；最多5更新 | 旧预检保留；R1纠正毫米单位分类和Eq.(4)构造后以2个Adam更新通过，恒定1e-4、fresh目录、checkpoint和合同完整 | PASS；不计科学成绩 |
| PAPER01-S1 | 32³、1000样本、四级、Adam、恒定1e-4、等效batch32、25000更新 | 回传审计PASS：history=25000、恒定lr、fresh随机初始化、contract/checkpoint哈希均通过；科学FAIL：宏nMAE=9.2334%，relL2 p90=57.9028%，Eq.(5) MRE=2.0180 | 交付PASS / 科学FAIL；不进入第二阶段 |
| PAPER01-DIAG | `PAPER01-S1` best checkpoint、200个保留样本、0更新分量/样本/角度/波数/幅值诊断 | 后5000更新test MSE仅降2.42%；curl-z平均nMAE=13.25%，x=7.49%，y=6.96%；`abs(cos theta)<0.25`组宏nMAE=13.45% | PASS；支持先查Eq.(4)、z分量归一化/支撑和近奇异角处理，不支持原样重训 |
| PAPER01-DATA-AUDIT | `sample_specs`与上一轮per-sample误差，0更新公式/归一化合同审计 | `k dot E0`相对最大≤1.23e-16，说明横向条件自洽；Ez放大比均值1.26、p90=3.09、最大7.89；近奇异角组Ez放大均值4.54且宏nMAE最高 | PASS；后续只支持小型角度/幅值/归一化消融，不支持直接长程 |
| PAPER01-ABLATION-SMOKE | 四臂短预算第一阶段消融：baseline、theta_min_0p5、ez_cap3、projected_amp；每臂2000 Adam | 服务器包`server_paper01_ablation_bundle.zip`已生成；用楼式进度输出和lab_log记录，跑完回传审计 | READY；只判断是否值得完整重训，不认证论文复现 |
| SR-G128 | 新轨迹全段残差、六分量、去源场和波形认证 | 失败转研究判断；通过才依首次更新前冻结的长程晋级合同执行 | BLOCKED |
| L1/G1024/L2/U | 原1024→8192及合格问题训练后的冻结复用目标 | 新合格128轨迹+累计预算/模型/优化器/RNG认证 | NOT_RUN；clean128科学FAIL，不启动 |

SR-SHORT冻结配方见[第二批协议](docs/plans/2026-09-15-server-batch2-protocol.md)，记录器修复见[修复记录](docs/plans/2026-09-15-server-batch2-recorder-fix.md)，有效回传审计见[SR-SHORT-R2回传审计](evidence/server_resource_v1/batch2_return_review.md)。已暴露S1R盲测转作诊断集；后续测试集必须首次更新前封存，不能用旧盲测挑模型。

TOL1E5-P首次更新前已冻结64/128条件、累计预算和晋级规则；有效R2未进入64，因此不能启动SR-G128、1024或8192。当前证据否定“简单收紧单步残差门即可救场”，不授权继续扫容差。SR-FAIL-AUDIT进一步把失败分成两层：首个E拟合存在1e-5优化/表达限制；旧残差128轨迹存在场传播误差积累。SR-E1-BUDGET已证明首个E目标在9000上限内可于3769 Adam达到1e-5，这支持“单步预算/优化速度不足”解释，但仍不恢复旧失败权重，也不构成连续轨迹通过。

## 验收不变

- 第一阶段同时列历史代码的宏平均nMAE≤1%、样本relL2 p90≤5%和原计划“逐样本≤1%”检查。归约差异必须公开，不能用较宽判据替代较严判据。Eq.(5)MRE独立列。
- 第二阶段非零半步R<1e-4；零靶规则不变。全段活跃去源Q≤5%，有效六分量nMAE≤1%，源外整段相对L2≤5%，A_fixed≤1e-3；弱场沿用原全时长固定参考和绝对门1e-5。
- 当前时刻弱参考与原全时长规则、旧审计门槛覆盖不完整等问题先做测量验证；旧布尔字段不能升级为PASS。
- 1024覆盖300/600/900切片和增长检查；8192五模频率对解析≤0.5%、对匹配FDTD≤0.2%，峰幅≤1dB，活跃能量偏差≤10%。
- 同机同精度P/R两随机对照的Adam和耗时均节省≥20%才支持收益；预训练/评估/失败成本分列。精确Yee、临时吞吐模型不计DCO成绩。

## 执行和交付

当前优先：[DCO价值回顾](docs/reports/2026-09-16-dco-value-retrospective.md)。纠正此前“完全按原文配置”的表述：S1R规模对齐但采用cellsize/RMS/余弦调度；当前P使用旧lr1e3_300。论文loss与项目R尚未等价确认，历史“论文式1e-4”只是项目R取1e-4的诊断。以下为保留的队列来历，本轮不按其中旧下一步自动开训。

本地先行已完成：三次只读行动共0参数更新；五臂JSONL成本与summary一致，8个里程碑指标按原参考精度读回一致。四条旧128轨迹记录中的当前Q在57–58步首次超过5%，有效分量nMAE在66步首次超过1%，不能把残差128解释为完整全时长场认证。S1R 32³已见测试只有1/16样本宏nMAE≤1%，最差样本贡献52.71%的宏误差和。服务器回传显示旧lr1e3在同题四网格均优于S1R，GV100推荐microbatch=8。SR-SHORT-R2回传显示tol=1e-5在第一个E_pending即失败，0个完整接受步；SR-E1-BUDGET回传显示同一首个E目标在3769 Adam达到1e-5；SR-MICRO4回传显示4步严格微型推进PASS_MICRO；SR-MICRO16回传显示16步严格微型推进PASS_MICRO；SR-64回传FAIL，停在第36步E半步拟合失败。低学习率clean64回传PASS_64；clean128完成128步但科学FAIL，失败点为Ex/Ey分量nMAE超过1%而非总Q超过5%。论文式1e-4诊断在64步Q=9.041%失败；随机初始化同规则64步PASS且成本低于预训练clean64，当前证据不支持预训练收益。PAPER01数据合同审计说明第一阶段参考线的横向平面波公式自洽，风险集中在未公开的角度奇异处理、幅值尺度和归一化定义。以上仍不构成128/1024/8192通过。

本次先交付诊断代码、测试、可追溯本地结果和服务器启动包。当前会话无远程连接时由用户在服务器执行短命令，明确哪些已执行、哪些待运行。

部署只新增本轮任务，不覆盖服务器 `project/actions.jsonl` 或 `records/`。已登记任务不自动重跑。每项证据审核后同步PLAN、STATUS、机器计划；科学FAIL与交付状态分列。旧详细协议仍约束旧实验，本批以后续新协议为准。
