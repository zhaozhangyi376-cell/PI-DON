# SR-DESIGN 报告

状态：交付PASS。科学状态：NOT_APPLICABLE。

## 已审阅证据

- 服务器S1R完整性审计PASS：1000连续epoch、每轮25次更新、25000总更新、best/last optimizer step、stage/summary/audit/lab hash均一致。
- S1R恢复资格仍为false：训练预算已完成，且独立shuffle generator状态未保存；不允许追加预算。
- 三模型同题诊断中，旧`dco_lr1e3_300.pt`在四个网格上均优于S1R_best。
- GV100吞吐诊断推荐microbatch=8；4/8/16首步等价均通过，8最快。

## 关键判断

S1R不是“没跑完”，而是完整跑完后科学盲测失败。旧`lr1e3_300`模型更好，说明当前最有价值的问题不是重复S1R，而是解释第二阶段为什么“残差128通过但场门失败”。

旧M2已经使用`dco_lr1e3_300.pt`作为预训练初始化，并且A-P/B-P能完成128个残差接受步但场门失败。因此下一批不应该再证明“旧模型能跑残差”，而应该测试：把单步残差门从`1e-4`收紧到`1e-5`后，场误差是否仍在短程内越线。

## 冻结的下一步

启动一个独立在线短程干预：

- 名称：`TOL1E5-P`
- 任务：`SR-SHORT`
- 脚本：`scripts/experiments/server_short_tol_probe.py`
- 初始权重：`assets/models/dco_lr1e3_300.pt`
- 目标：先64步；64步全场门通过才同一预算继续128
- 总预算：Adam<=150000，closure=0，per_fit<=3000
- 推荐microbatch：此在线脚本不是S1批训练，不直接使用SR-PERF microbatch；SR-PERF只证明服务器GPU可用和等效批次稳定。

## 暂不启动的内容

- 不重复S1R。
- 不启动新的S1长训练，除非后续需要正式复现旧`lr1e3_300`训练口径作为第一阶段交付。
- 不启动1024/8192。
- 不把精确Yee或诊断控制计作DCO成绩。

## 后继规则

如果`TOL1E5-P`在64步失败：第二阶段仍不能进入长程，下一步应检查接口、边界、弱参考和训练分布。

如果`TOL1E5-P`在64步通过但128失败：可登记SR-G128前的128复核，不启动1024。

如果`TOL1E5-P`在128步也通过：才允许登记SR-G128审计；SR-G128审核通过后，才讨论1024。

