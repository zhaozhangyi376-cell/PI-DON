# 协议附录 02：统一波数元数据访问器

创建时间：2026-09-14，发生在`lab_log #229`失败之后、任何`phase1_raw.json`或第二阶段参数更新之前。

`#229`证明附录01只修复了采样入口：评估汇总仍直接读取`spec["k_rad_per_m"]`，使论文固定波形在写入结果前以同一类`KeyError`退出。根因是两种已登记元数据模式没有在入口统一。

最小修复为`wave_numbers(spec)`；它只返回`k_rad_per_m`或等价的`k`，并由采样和汇总共同调用。失败证明为#230（缺少访问器），通过证明为#231（两个模式均通过）。

| 评估器版本 | SHA256 |
|---|---|
| 附录01后、#229使用 | `e9fabee2dfbe4b2aff8dc18da1eaa2027e02310a7ba1195b8875a1919b0c6197` |
| 本次实际结果使用 | `1f7a1bba4ab1a59858b1f11bc1d8da46c8777c9929af6bf5afb5dbd7d0be040d` |

不改变波数值、物理采样、网络、归一化、门槛或任何训练设置。#226/#229的失败、初始源码快照及附录01全部保留。
