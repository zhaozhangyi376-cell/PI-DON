# 执行偏差 01：随机初始化R臂被代理提前终止

关联运行：`lab_log #234`。本文件不是把该运行重写为失败或完成，而是说明它实际留下的证据边界。

- R臂已持久写入26行连续、`accepted=true`的JSONL记录（time layer 0–25）；每行的H/E拟合均由严格`R<1e-4`接受。
- 第0层：R的curl-E在95次Adam更新后`R=9.9136506e-5`；相同层的P臂在500次后`R=5.1602571e-4`而未接受。
- 代理在总运行约318.6秒时观察到一次空目录轮询，错误地把它解释为“尚未产生日志”，并把每半步360秒上限错误套用到多步累计时长，终止了进程。
- 终止前每个落盘半步本身未超过360秒；终止不是由本协议的资源门槛触发。`lab_log #234`记录退出码4294967295。
- 运行器只在64/128步写检查点，故26步的网络/优化器状态未保存，不能严格恢复，也不能以重跑补领同一轨迹的预算。

处理：R记为`PARTIAL_ABORTED_BY_AGENT`，已落盘26步仅作描述性证据，不能用作完整B（预训练收益）对照、S2或U成绩。P的严格失败和第一阶段结果不受此偏差改变。后续不在本协议下重跑R；如要恢复随机对照，必须由新协议明确登记，并将每个已接受步的恢复点作为实现前置。
