--- scripts/analysis/verify_claims.py
+++ evidence/workspace_reorganization_20260914/before/root/verify_claims.py
@@ -25,16 +25,8 @@
 
     RESULTS.md and small JSON manifests can be versioned. Large .pt/.npz
     artifacts are ignored; hashes and source snapshots identify local evidence.
 """
-
-# Project layout bootstrap (imports and paths only).
-import sys as _layout_sys
-from pathlib import Path as _LayoutPath
-_layout_sys.path.insert(0, str(_LayoutPath(__file__).resolve().parents[2]))
-from project_paths import ROOT as PROJECT_ROOT, configure as _layout_configure, resolve_legacy
-_layout_configure()
-
 
 import argparse
 import glob
 import hashlib
@@ -1285,9 +1277,8 @@
 ]
 
 
 def run_claim(cid, name, cmd, pat, ok_fn, crit):
-    cmd = [cmd[0], str(resolve_legacy(cmd[1])), *cmd[2:]]
     if not os.path.exists(cmd[1]):
         return "NODATA", "缺脚本", f"{cmd[1]} 不在当前目录"
     try:
         p = subprocess.run(cmd, capture_output=True, text=True,
