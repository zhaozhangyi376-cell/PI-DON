--- scripts/analysis/audit_mechanism_hour.py
+++ evidence/workspace_reorganization_20260914/before/root/audit_mechanism_hour.py
@@ -1,18 +1,10 @@
 """Independent audit for the bounded mechanism experiment."""
 from __future__ import annotations
-
-# Project layout bootstrap (imports and paths only).
-import sys as _layout_sys
-from pathlib import Path as _LayoutPath
-_layout_sys.path.insert(0, str(_LayoutPath(__file__).resolve().parents[2]))
-from project_paths import ROOT as PROJECT_ROOT, configure as _layout_configure, resolve_legacy
-_layout_configure()
-
 import hashlib, json
 from pathlib import Path
 
-ROOT = PROJECT_ROOT
+ROOT = Path(__file__).resolve().parent
 EVID = ROOT / "evidence" / "mechanism_1h_v2"
 
 def sha256(p: Path) -> str:
     h = hashlib.sha256()
