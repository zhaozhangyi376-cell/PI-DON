--- scripts/exploratory/structured_scale.py
+++ evidence/workspace_reorganization_20260914/before/root/structured_scale.py
@@ -50,16 +50,8 @@
        separability is the entire claim of this file.
     A failure on 2 or 3 is a real result: it would say the anisotropy is not
     where the remaining error lives.
 """
-
-# Project layout bootstrap (imports and paths only).
-import sys as _layout_sys
-from pathlib import Path as _LayoutPath
-_layout_sys.path.insert(0, str(_LayoutPath(__file__).resolve().parents[2]))
-from project_paths import ROOT as PROJECT_ROOT, configure as _layout_configure, resolve_legacy
-_layout_configure()
-
 
 import argparse
 
 import numpy as np
