--- scripts/experiments/gap_analysis.py
+++ evidence/workspace_reorganization_20260914/before/root/gap_analysis.py
@@ -27,16 +27,8 @@
 
 Everything printed here is measured, except the 32^3/64^3 projection in (B),
 which is explicitly labelled an extrapolation and states its assumption.
 """
-
-# Project layout bootstrap (imports and paths only).
-import sys as _layout_sys
-from pathlib import Path as _LayoutPath
-_layout_sys.path.insert(0, str(_LayoutPath(__file__).resolve().parents[2]))
-from project_paths import ROOT as PROJECT_ROOT, configure as _layout_configure, resolve_legacy
-_layout_configure()
-
 
 import argparse
 import json
 
