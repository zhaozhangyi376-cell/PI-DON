--- scripts/exploratory/mini2d.py
+++ evidence/workspace_reorganization_20260914/before/root/mini2d.py
@@ -35,16 +35,8 @@
     here is the exact miniature of the 3-D vector-potential idea: it makes
     div(curl) = 0 structural instead of penalised.  Same question, one tenth
     the compute.
 """
-
-# Project layout bootstrap (imports and paths only).
-import sys as _layout_sys
-from pathlib import Path as _LayoutPath
-_layout_sys.path.insert(0, str(_LayoutPath(__file__).resolve().parents[2]))
-from project_paths import ROOT as PROJECT_ROOT, configure as _layout_configure, resolve_legacy
-_layout_configure()
-
 
 import numpy as np
 import torch
 import torch.nn as nn
