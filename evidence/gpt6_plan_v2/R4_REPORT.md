# R4：开发状态与 P4-A 结论

## 有效开发状态实验

run #129 使用主 checkpoint `dco_lr1e3_300.pt`（SHA256 `3f259bc887a10fac…`），每个候选从该权重重新初始化。无更新基线明确为 `max_inner=0`，候选只用 Adam、lr=`3e-4`、最多500次更新。第43与96步按 E→source→H 的 FDTD 状态构造；curl-E 仅在预测 curl-H 通过后才允许开始。

| 时间步 | curl-H 初始 R | curl-H 500 次最终 R | curl-H 通过 | curl-E | 固定幅值误差 | 状态 |
|---:|---:|---:|---|---|---:|---|
| 43 | `4.428806e+01` | `1.278614e-03` | 否 | 未开始 | `1.193850e-04`（未完成单步，不能作 G1 成绩） | before_H |
| 96 | `6.446421e+00` | `9.224233e-04` | 否 | 未开始 | `7.331579e-05`（未完成单步，不能作 G1 成绩） | before_H |

两项都超过冻结的 `R<1e-4`。第43/96处的 A_fixed 与 Q 只是未完成事务的诊断，不以它们绕过内层停止门槛。

## 保留的无效首次运行

run #126 的目录 `R4_development/` 保留，但不计入判定：其“无更新基线”错误使用了500次更新，随后候选复用了该网络。这是实现污染，不是科学上的通过或失败。run #127 复现问题，run #128 的零预算/全新候选单元测试通过，随后才执行有效 run #129。

## 唯一 P4-A 分支

run #132 在第43步 curl-H、同一初始权重上执行预登记比较：

| 配置 | Adam更新 | LBFGS调用 / closure | 最终 R | 门槛 |
|---|---:|---:|---:|---:|
| Adam | 200 | 0 / 0 | `7.412442e-03` | `<1e-4` |
| Adam + LBFGS | 200 | 97 / 200 | `5.061260e-04` | `<1e-4` |

LBFGS 降低了残差，但未达到门槛。总运行38秒，未触及60秒/目标或60分钟阶段预算。参数和优化器状态保存在 `R4_p4a_step0043/`；若未来需要核对，不能重新获得 closure 预算。

## 判定

**G1 FAIL。** 允许的一次纠正性 P4-A 已用完且无合格开发候选。因此 16/300/600与192/450/900固定验证、64/128/1024/8192闭环、P8 均为 **NOT_RUN**，不是它们各自的 FAIL。

R3 的正确四样本宏 nMAE 为 `2.006568e-02`，未满足 `1e-3`；同时没有发现实际数据、单位、位置或梯度的可复现实现缺陷。P5 的“修复后重试/配对新小试/1000 epoch”前置不成立，标为 **NOT_RUN**。继续需要新的、单独登记的结构或训练假设；本计划不允许把预算加长、改门槛或直接续跑长程。
