# 待确认：间距覆盖的有限预算A/B修复对照

状态：仅为下一里程碑方案，尚未训练。依据run #23的坐标分支干预与间距扫描结果制定。

问题：保持当前网络和损失，扩大间距覆盖能否改善已定位的错误依赖？这是诊断增强版，不是论文原训练设置的严格复现。

| 项目 | 固定设置 |
|---|---|
| 起点 | 两组都从dco_lr1e3_300.pt的同一SHA256开始，原权重不覆盖 |
| 唯一实验因素 | A：每轴U[0.3,0.8]mm；B：每轴U[0.2,1.2]mm |
| 配对数据 | 128组32³波场；共同随机种子20260915；同一方向、波数、振幅及间距均匀随机数u，分别映射到两个范围 |
| 波场分布 | 保持当前生成器的4–16波、shared方向、角度拒绝阈值、解析Yee位置旋度与零相位；不混入单波探针 |
| 训练 | 相同batch=4、Adam lr=1e-4、相同batch索引序列；不更改结构/编码/RMS/相对MSE损失 |
| 预算 | 每组200次参数更新；不自行延长或搜索学习率；出现非有限值时停止并记录 |
| 保存 | 分别保存最终权重、完整配置、每10次更新的损失与耗时；记录数据/起点/产物哈希和代码快照 |

验收用未参与本轮结论选择的振幅种子10、11、12，固定19.2mm物理窗口、四种原Fig.5/6重建网格。同时重评起点权重A0，A200与B200只在完全相同输入/真值上比较；坐标干预探针全部复测。

预先规定本预算内的改善门槛：

1. 两种非立方网格×三个新种子，共六案例：B200/A200的逐分量平均nMAE比值和x分量式（5）MRE比值，分别取几何平均，均须≤0.8。
2. 原范围内0.6mm、32³的三个新种子：B200平均nMAE不得比A0退步超过10%。
3. 单波六种方向/极化各自最大横向坐标干预效应，再取六者平均：B200不得高于A0。此项衡量错误依赖，不等于场精度。

完整报告各案例、所有指标及不利结果，不只报汇总；上面的nMAE门槛属于修复诊断，不能替代论文MRE验收。200次更新未通过只说明这个预算和配置下未见足够改善，不能证明扩大覆盖普遍无效。

若通过，下一里程碑讨论更充分训练及原论文路径的对照；若未通过，结合更新曲线决定是否检查编码或引入明确的物理约束。两组运行及阶段验收后停止，不直接进入Algorithm 1。
