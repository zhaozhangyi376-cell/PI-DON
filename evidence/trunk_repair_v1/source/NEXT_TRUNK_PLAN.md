# 待确认：冻结其余模块，只更新坐标分支

状态：方案已整理，尚未运行。上一轮A/B整体修复未通过；本轮不追加更新，不进入Algorithm 1。

问题：使用同一扩展数据时，把可更新参数限制在坐标分支，能否减轻错误间距依赖并减少原范围内的精度退步？只更新trunk仍可能改变全部输出，不保证保留精度。

| 项目 | 固定设置 |
|---|---|
| 起点 | 与A200/B200相同的原权重dco_lr1e3_300.pt，SHA256以本目录manifest.json为准 |
| 唯一新增处理 | C200仅允许参数名以trunk.开头的参数更新；branch、up、dec、head全部冻结 |
| 数据 | 直接复用run #27的train_B200.npz，并核验其哈希；不重抽样、不混合数据 |
| 训练顺序 | 复用run #27的200×4 batch索引序列 |
| 优化与预算 | 新建Adam，lr=1e-4、batch=4、逐样本相对平方损失；恰好200次更新，无调参、调度器或追加预算 |
| 对照 | 原A0及已完成的A200/B200；B与C的差别是允许更新的模块集合 |
| 完整性 | 记录可训练参数名与数量；逐个核验冻结参数训练前后完全相同，可训练参数的实际Adam step均为200 |
| 保存 | 独立C200权重、优化器状态、200条曲线、源/数据/结果哈希，运行包lab_log |

验收分两层：

1. 对已有种子10/11/12与30个坐标探针补测C，用于和本轮证据直接对照。这批种子已看过，明确标作诊断集，不再称独立终验。
2. C完成后一次性用新振幅种子20/21/22，固定同一19.2mm窗口与四网格，统一评估A0/A200/B200/C200，共48组完整预测。训练期间不查看这些结果。

新种子的预设门槛沿用上一轮严格程度：

- 六个非立方案例，C200/A200的分量平均nMAE比和x分量式（5）MRE比分别取几何平均，两者均≤0.8。
- 32³三个种子平均nMAE，C200/A0≤1.1。
- 六个方向各自最大坐标干预效应再取平均，C200/A0≤1。探针是已知诊断集，不宣称独立泛化。

三项均通过才称“本预算的修复门槛通过”，仍不等于论文严格复现。附报C/B全部比值，不能只挑有利指标；新种子下B/A0的变化也要展示，以区分测试种子影响。仅一个训练随机种子，不能据单次结果推断普遍因果规律。

本方案不修改架构、坐标表达、归一化或目标。若失败，记录失败并停止验收，不自动延长训练。是否再改物理结构或回到论文坐标/归一化设计，留待下一次基于证据的决定。
