# P5 配对 A/B 小试

两个方案从同一随机 L4/base32 初始状态开始，使用相同的 200 个 batch（每 batch 4 个样本）、相同的 128 训练样本和 32 个未参与更新的开发样本。A 是现有相对损失；B 是按每个训练样本/分量真值局部最大值加权的物理量损失。B 的尺度只进入训练损失，不进入推理还原。

| 方案 | 开发宏 nMAE：训练前 | 开发宏 nMAE：200 更新后 |
|---|---:|---:|
| A | 5.142061e-01 | 2.126001e-01 |
| B | 5.142061e-01 | 8.271047e-01 |

该表仅用于判断是否值得进入完整 P5 验收；不得以此选择结果宣称 Fig.6 改善或论文复现。
