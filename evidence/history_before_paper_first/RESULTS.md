# 复现结论核对表

> `verify_claims.py` 自动生成于 2026-09-12 17:33，commit `dad6fb2`。**不要手工编辑。**
>
> 每一行都是当场从磁盘上的产物重算的。判据是事先写死在脚本里的，不是看到结果再定的。
> 想自己验：`py -3.11 verify_claims.py --run --md`，应该得到同样的表。
> 产物文件（.pt/.npz/.json，好几个 GB）被 .gitignore 挡在外面，所以仓库里看不到数据 —— 这张表就是能进仓库的那份证据，
> 每次运行的命令、commit、耗时和输出文件的 sha256 在 `LAB_NOTEBOOK.md`。

| | 结论 | 复算值 | 判据 |
|---|---|---|---|
| ✅ | **C1** 谱半径探针可信：精确 Yee 旋度测出来 rho = 1 | \|rho_exact - 1\| <= 2.00e-05 | 201 行里精确 Yee 旋度的谱半径都贴着 1；阈值 1e-3 |
| ✅ | **C2** rho 能预测闭环能跑多少步 | corr = +0.894 | 192 个点，16 个 checkpoint 中 14 个自身 corr>0.8；阈值 0.8 |
| ✅ | **C3** 发散发生在固定物理时间，不是固定步数 | 斜率 b 中位数 -1.017（固定物理时间 = -1，固定步数 = 0），16/16 个落在 [-1.15,-0.85] | 判据：\|中位数 +1\| <= 0.15 且 >=75% 在带内（实得 100%）。b 的范围 -1.03~-1.00；同一 CFL 下换 seed 的寿命本身就差 1.20x，这是噪声底，不是效应 |
| ✅ | **C4** 精度只通过 rho 影响稳定性 | 精度\|固定rho: -0.078　rho\|固定精度: -0.819 | 13 个 checkpoint。判据：rho 的偏相关绝对值更大，即精度只是通过 rho 起作用 |
| ✅ | **C5** 规模可以替代物理损失 | dco_paper32 rho-1=0.1149　pidon_R2_all rho-1=0.1234　比值 1.074x | 阈值 1.15x。打平 = 纯监督的规模能顶上全套物理损失 |
| ✅ | **C6** 论文没写明的实现细节同时伤精度和稳定性 | dco_L3b rho-1=0.5028　dco_L3d rho-1=0.2479　差 2.03x | 两个实现开关（trunk 坐标编码 / 归一化）同时影响精度和稳定性。注意口径更正（2026-09-10 读原文后）：这两点论文其实都写了 —— III-A「trunk decodes coordinate information into cell sizes」指定了 cellsize 编码，III-B「output normalized by the local maximum for each component」指定了按分量的输出归一化。所以 dco_L3b（abs 坐标）不是「论文字面读法」，那个标签是错的，已撤。本条现在只主张我们自己消融里的敏感度，不再主张论文有遗漏 |
| ⚠️ | **C7** 反例：消融链终点在 rho 上反而更差 | dco_L3c rho-1=0.1812 < dco_L3d rho-1=0.2479 | 这是反例，不是支持项。消融链终点在 rho 上反而更差，讲的时候要一起说 |
| ✅ | **C8** 无约束学习算子做不了长时程积分 | 最好 dco_paper32: rho-1=0.1149，要跑 1e5 步需再小 11,491x | 寿命约为 1/(rho-1)，所以目标步数 N 需要 rho-1 ~ 1/N。　实测：唯一一次训练网格加倍（pidon_R1_roll 16^3 rho-1=0.1172 -> dco_paper32 32^3 rho-1=0.1149）只带来 1.02x 改善。　按实测倍数外推，规模路线要跨过 4 个数量级并无依据 —— 两周后的论文规模复现就是对「规模能否解决发散」的直接检验。这条不是失败，是本复现最有价值的负面结论 |
| ✅ | **C9** 换网格的精度，和论文同口径比在 3x 以内 | dco_paper32（训练于 32^3）最差 1.50x | 判据 <3x，用 MAE/max（nMAE）口径，只认 dco_paper32 —— 它是按论文配置在 32^3 上训的，同起点才能比。64^3: 2.88e-03 vs 4.1e-03 = 0.70x　64x96x16: 5.69e-03 vs 3.8e-03 = 1.50x　32x64x16: 6.27e-03 vs 4.7e-03 = 1.33x。　口径依据：式(5)字面实现在同一批数据上给出 0.16~0.77，是论文 4e-3 的 40~188 倍 —— 旋度过零处逐点比值发散，字面读法不可能是论文的数；III-B 的「按分量局部最大值归一化」把分母变成局部最大值，即 MAE/max，故本条用 nMAE　　其余（训练网格不同，不参与判定）：dco_L4b(训练于 ?^3) 最差 13.35x；pidon_R2_all(训练于 ?^3) 最差 2.14x　先前「差 10x」是跨口径比出来的，已撤回 |
| ✅ | **C10** 相对 L2 与 nMAE 不可互换（上一条曾因此报错） | relative L2 / nMAE = 2.6~8.1x（5 个 checkpoint） | 判据：最小比值 >2，即两个口径处处相差一倍以上，不可互换。逐 checkpoint：dco_L4b 3.7-5.6x；dco_lr1e3_300 3.0-6.0x；dco_lr1e3_b8_300 3.1-6.6x；dco_paper32 4.7-8.1x；pidon_R2_all 2.6-4.5x —— 倍数本身就不是常数，所以任何记下来的固定倍数都不能用 |
| ✅ | **R1** 精确旋度就是一个 12 权重的 3x3x3 卷积 | 1.166e-16 | relative L2 < 1e-12（等价于机器精度，说明 2.25M 参数在学一个 12 参数的东西） |
| ✅ | **R2** 自己写的 FDTD 参考解对得上解析解 | -0.10, -0.12, -0.10, -0.30, -0.20 | 每个模式 \|误差\| < 0.5% |
| ✅ | **R4** PEC 边界下伴随恒等式 curl_H = curl_E^T 仍精确成立 | 0.000e+00 | 三明治结构的全部前提。此前只在周期盒验证过，而论文的腔体是 PEC；若此式不成立，成对替换在腔体里就要先补边界修正 |
| ✅ | **R5** 介质界面下成对替换的保证仍成立，单旋度替换仍不成立 | RESULT material_pair_ok 1, RESULT material_onesided_ok 0 | 两条都要命中才算过：成对的成立、单侧的不成立。论文 Fig 8 本身含 eps_r=4 的 inhomogeneous cavity |
| ✅ | **R3** 谱半径自检（现算，不看缓存） | PASSED | 精确旋度 rho≈1 且倾斜旋度 rho>1，两个方向都要对 |
