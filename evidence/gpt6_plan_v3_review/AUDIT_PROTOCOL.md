# v2 停止点复核登记

本轮由用户“又遇到停止条件了，检查，调整”授权。这里只做源码审查、CPU 微型故障复现和既有权重无更新推理，不重开任何正式 DCO 训练预算。

登记于 `audit_v2_stop.py` 运行之前。保留 v1/v2 所有原始报告、权重及失败记录。输出写入本目录，已有同名结果拒绝覆盖。

1. 累计时间已达上限时，同一子步新增 Adam 更新应为 0；用 n=3、L2/base2 的 CPU 微型网络验证，不计 DCO 训练成果。
2. 恢复改变 LBFGS closure 额度应被拒绝；不同运行身份写入同目录应被拒绝。仅用临时目录与微型状态。
3. LBFGS 在第二次调用中断时，应恢复第一次完整接受调用后的状态，而非退回全部 LBFGS 之前。用可控测试替身验证控制流，不计实际 LBFGS 训练。
4. 重载 v2 的 43/96 Adam500 和 43 Adam200、Adam200+LBFGS 最终权重，在保存的实际输入及相同支撑上纯推理复算 R。保存物理 SSE、分量 R、目标能量、优化器步数、checkpoint 计数与真实结构配置。R 门槛仍为严格 <1e-4，不调整。
5. 主模型在第43步 H/E 的共同振幅 1e-3/1/1e3 无更新诊断，报告比例还原误差及归一化 floor。只诊断，不据此晋级。
6. 原 PECCavity.step(H→E→source) 保持独立演进，测量副本补 H 半步后，与已有 E→source→H 参考逐步比较128步；误差阈值沿用 double<=1e-10。只认证二者参考时间层等价，不替代完整 G0。
7. P5 四个实际样本检查保存元数据、shape、有限性、归一化往返、优化器步数；缺少生成参数时标签来源标 INCOMPLETE，不推定正确或错误。

本轮预期是获得可靠分类，而非保证收敛。工程缺口只授权修复该缺口，不能自动重新获得500更新或200 closure。

## 追加的几何诊断（在执行前登记）

针对 `gen_data.py` 的 E/C 交错位置与 `Solver` 的 H 输入，验证现有 `h_shift=True` 是否使预训练布局的精确前向差分核等于 `fdtd.curl_H`。n=7、各向异性间距(0.1,0.2,0.3)、固定随机seed=20260913，完整有效支持；double相对L2<=1e-10。对照为现有未shift输入。该测试只认证位置语义，不认证真实神经网络收敛；精确核输出不能进入生产DCO更新。
